# float em decimal
# :io_lib.format("~.6f", [123.0])
IO.puts trunc(123.456)
# retornar extessão do arquivo

# file name
Path.expand("~/convert")
System.cwd()

# Valores do sistema
System.version()
System.monotonic_time()
System.os_time()

# process's current working